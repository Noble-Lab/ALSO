git checkout branch_Gang
# commits
git push
git status

git rm bin/workflow/Gang-git
#git commit bin/workflow/Gang-git -m "mv to src"


# git add src
# git commit src -m "src folder"

# git add src/Gang-git
# git commit src/Gang-git -m "Gang's git code"

# git add src/Gang-test-preprocess-0406.sh
# git commit src/Gang-test-preprocess-0406.sh -m "Gang tested preprocess pipeline with -f3 option. "
git config --global alias.add-commit '!git add -A && git commit'
git add Gang-git
git add-commit -m 'add src folder to store the debugging script for Gang'

git checkout main
git pull 
git checkout branch_Gang
git merge main
git status
git push

git checkout main